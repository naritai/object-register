import {
	height as videoHeight,
	width as videoWidth
} from "../VideoElement/params";

export const canvasWidth = videoWidth - 8;
export const canvasHeight = videoHeight - 80;
export const svgFieldId = "super-canvas";
